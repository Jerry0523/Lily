<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@drawable/list_head_background"
    android:orientation="vertical" >

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="50dip"
        android:background="@drawable/title_background" >

        <Button
            android:id="@+id/imageview_bbutton"
            style="@style/TitleLeftBackButton"
            android:text="@string/back" />

        <ImageButton
            android:id="@+id/imageview_save"
            android:src="@drawable/title_image_save"
            android:contentDescription="@string/save"
            style="@style/TitleRightImageButton" />
    </RelativeLayout>

    <LinearLayout
        android:id="@+id/imageview_body"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_gravity="center" />

</LinearLayout>